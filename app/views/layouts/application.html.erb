<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/assets/favicon.ico" rel="shortcut icon">
    <link rel="apple-touch-icon" href="/assets/apple-touch-icon.png">
    <link rel="apple-touch-icon" sizes="72x72" href="/assets/apple-touch-icon-72x72.png">
    <link rel="apple-touch-icon" sizes="114x114" href="/assets/apple-touch-icon-114x114.png">
    <title><%= content_for?(:title) ? yield(:title) : "CNGEI Firenze - Campi estivi" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>

    <%= render('layouts/navbar') %>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span3">
          <div class="well sidebar-nav">
            <% if current_user %>
              <ul class="nav nav-list">
                <li class="nav-header"><%=t '.sidebar.title', :default => "Campi" %></li>
                <% Camp.all.each do |camp| %>
                  <li><%= link_to camp.title, camp_path(camp) %></li>
                <% end %>
              </ul>
            <% end %>
          </div><!--/.well -->
        </div><!--/span-->
        <div class="span9">
          <%= content_for?(:breadcrumb) ? yield(:breadcrumb) : render('layouts/breadcrumb') %>

          <% if flash[:info] %>
      	    <div class="alert alert-info fade in" data-alert="alert">
              <a class="close" data-dismiss='alert' href="#">×</a>
              <h4 class="alert-heading"><%=t '.info', :default => "Info" %></h4>
              <p><%= flash[:info] %></p>
            </div>
      	  <% end %>
      	  <% if flash[:warning] %>
      	    <div class="alert fade in" data-alert="alert">
              <a class="close" data-dismiss='alert' href="#">×</a>
              <h4 class="alert-heading"><%=t '.warning', :default => "Warning!" %></h4>
              <p><%= flash[:warning] %></p>
            </div>
      	  <% end %>
      	  <% if flash[:error] %>
      	    <div class="alert alert-error fade in" data-alert="alert">
              <a class="close" data-dismiss='alert' href="#">×</a>
              <h4 class="alert-heading"><%=t '.error', :default => "Error!" %></h4>
              <p><%= flash[:error] %></p>
            </div>
      	  <% end %>
      	  <% if flash[:alert] %>
      	    <div class="alert alert-error fade in" data-alert="alert">
              <a class="close" data-dismiss='alert' href="#">×</a>
              <h4 class="alert-heading"><%=t '.alert', :default => "Alert!" %></h4>
              <p><%= flash[:alert] %></p>
            </div>
      	  <% end %>
      	  <% if flash[:notice] %>
      	    <div class="alert alert-success fade in" data-alert="alert">
              <a class="close" data-dismiss='alert' href="#">×</a>
              <p><%= flash[:notice] %></p>
            </div>
      	  <% end %>
      	  <% if flash[:success] %>
      	    <div class="alert alert-success fade in" data-alert="alert">
              <a class="close" data-dismiss='alert' href="#">×</a>
              <h4 class="alert-heading"><%=t '.success', :default => "Success!" %></h4>
              <p><%= flash[:success] %></p>
            </div>
      	  <% end %>

          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p><strong><%= link_to raw("Copyright &copy;"), "http://www.gnu.org/licenses/gpl.txt", :target => :blank %> <%= Time.now.year %> <%= link_to "CNGEI Firenze", "http://www.scoutfirenze.it", :target => :blank %></strong></p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
  </body>
</html>
